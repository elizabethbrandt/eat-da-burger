<h1>Eat Da Burgers!!</h1>

<h2>Burgers awaiting devourment</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> burger-waiting devoured=false}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers in my bellay!</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burger-devoured devoured=true}}
        {{/if}}
    {{/each}}
</ul>

<h2>What's next on the menu?</h2>

<form>

    <div>

        <input type="text">

        <button type="submit">Add to your plate</button>

    </div>

</form>